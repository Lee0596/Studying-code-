#include <stdio.h>
#include <math.h>

int main( )
{
	int  n,i,a,b;
	printf("输入行数\n");
	scanf("%d", &n);
	for (a = 1; a <= i; a++) {
		b = i - a;
		for (b; b >= 1; b--) {
			printf(" ");
		}
		for (n = 1; n <= (2 * a) - 1; n++) {
			printf("*");
		}
		printf("\n");

	

	return 0;
}

